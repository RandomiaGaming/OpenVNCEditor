using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CharacterRenderrer : MonoBehaviour
{
    /*public Character character = null;
    public GameObject linePrefab;

    void Update()
    {
        for (int i = 0; i < transform.childCount; i++)
        {
            Destroy(transform.GetChild(i).gameObject);
        }
        foreach(LineSegment lineSegment in character.lineSegments)
        {
            GameObject instantiatedLine = Instantiate(linePrefab, transform);
            instantiatedLine.GetComponent<LineRenderer>().SetPositions(new Vector3[] { (Vector2)lineSegment.startPosition, (Vector2)lineSegment.endPosition });
        }
    }*/
}
